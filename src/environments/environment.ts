export const environment = {
  production: false,
  envName: 'development',
  apiUrl: 'https://localhost:7001/api',
  tokenRefreshThresholdMinutes: 2, // minutos antes de expirar el access token para intentar refresh
};
